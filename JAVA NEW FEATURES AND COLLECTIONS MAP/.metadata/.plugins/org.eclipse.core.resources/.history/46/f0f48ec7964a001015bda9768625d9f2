package ArrayListExample;

public class RemoveAllDuplicateArrayList {

	public static void main(String[] args) {
		
		List<Integer> list = new ArrayList<>(Arrays.asList(1, 2, 2, 3, 4, 4, 5));
		Set<Integer> set = new LinkedHashSet<>(list);
		list.clear();
		list.addAll(set);

		
	}
}
