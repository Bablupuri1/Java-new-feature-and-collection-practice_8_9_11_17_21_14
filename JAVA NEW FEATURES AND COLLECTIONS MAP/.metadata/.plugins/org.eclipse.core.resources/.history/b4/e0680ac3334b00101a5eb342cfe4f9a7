package Comrator_Example_Pratcing;
import java.util.*;

// User class
class User1 {
    int id;
    String name;
    int age;
    double salary;

    public User1(int id, String name, int age, double salary) {
        this.id = id;
        this.name = name;
        this.age = age;
        this.salary = salary;
    }

    @Override
    public String toString() {
        return "User{" +
                "id=" + id +
                ", name='" + name + '\'' +
                ", age=" + age +
                ", salary=" + salary +
                '}';
    }
}

public class Main {
    public static void main(String[] args) {
        List<User1> userList = new ArrayList<>();
        userList.add(new User1(3, "Alice", 25, 50000));
        userList.add(new User1(1, "Bob", 30, 60000));
        userList.add(new User1(2, "Charlie", 28, 55000));

        // ✅ Define Comparator reference for sorting by age
        Comparator<User1> ageComparator = new Comparator<User1>() {
            @Override
            public int compare(User u1, User u2) {
                return u1.age - u2.age; // Ascending order
            }
        };

        // ✅ Sort using the defined comparator
        Collections.sort(userList, ageComparator);

        // ✅ Print sorted list
        System.out.println("Sorted by Age:");
        for (User user : userList) {
            System.out.println(user);
        }
    }
}
